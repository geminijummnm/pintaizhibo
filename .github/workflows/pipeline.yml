name: M3U Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  generate-sources:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Generate source files
      env:
        BASE_URL: ${{ secrets.BASE_URL }}
        JSON_URL: ${{ secrets.JSON_URL }}
      run: |
        pip install -r requirements.txt
        python scripts/generate_sources.py

    - name: Validate sources directory
      run: |
        echo "✅ 生成的.url文件列表:"
        ls -la sources/
        echo "文件内容示例:"
        head -n 3 sources/*.url

    - uses: actions/upload-artifact@v4
      with:
        name: sources
        path: sources/
        retention-days: 1

  build-m3u:
    needs: generate-sources
    runs-on: ubuntu-latest
    env:
      LANG: C.UTF-8  # 强制中文编码
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/download-artifact@v4
      with:
        name: sources
        path: ./sources

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y jq

    - name: Process M3U
      shell: bash
      run: |
        echo "=== 环境验证 ==="
        echo "当前工作目录: $(pwd)"
        echo "sources目录内容:"
        ls -la sources/
        
        echo "=== 执行处理 ==="
        mkdir -p m3u
        chmod +x scripts/process_m3u.sh
        ./scripts/process_m3u.sh
        
        echo "=== 结果验证 ==="
        echo "生成的M3U文件列表:"
        find m3u/ -type f -print -exec head -n 3 {} \;

    - uses: actions/upload-artifact@v4
      with:
        name: m3u
        path: m3u/
        retention-days: 1
